'use client';

import { LoginCard, LoginSupportActions } from '@/features/auth';

export default function LoginPage() {
  return (
    <>
      <LoginCard />
      <LoginSupportActions />
    </>
  );
}
