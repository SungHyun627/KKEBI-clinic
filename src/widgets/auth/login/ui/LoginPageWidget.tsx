'use client';

import { LoginCard, LoginSupportActions } from '@/features/auth';

export default function LoginPageWidget() {
  return (
    <>
      <LoginCard />
      <LoginSupportActions />
    </>
  );
}
